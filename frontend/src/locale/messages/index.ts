import koMessage from './ko';
import enMessage from './en';

const messages = {
  ko: {
    // message: require('./ko').default,
    message: koMessage,
  },
  en: {
    // message: require('./en').default,
    message: enMessage,
  },
};

export default messages;
